rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype: allow all reads and writes (tighten before production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
